// Pure JavaScript, no TypeScript

// import { createUser } from "./user";

/**
 * 
 * @param {string} name 
 * @param {number} age 
 * @returns {string}
 */

export function createUser(name, age){
    return name + age
}