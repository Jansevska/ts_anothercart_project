//below is not a comment (once we turn on checkJS)
// this tells TS not to type check this file

//@ts-nocheck
function createUser(name, age) {
    return newUser
}

module.exports = {
    createUser
}